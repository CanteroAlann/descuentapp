{
  "name": "descuentapp-monorepo",
  "version": "1.0.0",
  "description": "Monorepo para DescuentApp - Clean Architecture & DDD",
  "scripts": {
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "docker:rebuild": "docker compose up -d --build",
    "docker:clean": "docker compose down -v",
    "backend:dev": "pnpm --filter descuentapp-backend dev",
    "backend:build": "pnpm --filter descuentapp-backend build",
    "backend:test": "pnpm --filter descuentapp-backend test",
    "frontend:start": "pnpm --filter descuentapp-frontend start",
    "frontend:android": "pnpm --filter descuentapp-frontend android",
    "frontend:ios": "pnpm --filter descuentapp-frontend ios",
    "install:all": "pnpm install -r"
  },
  "packageManager": "pnpm@8.15.0",
  "keywords": [
    "descuentapp",
    "monorepo",
    "clean-architecture",
    "ddd"
  ],
  "author": "",
  "license": "MIT",
  "private": true
}
