# Server Configuration
NODE_ENV=development
PORT=3000

# Logging (winston)
# error | warn | info | verbose | debug | silly
LOG_LEVEL=debug

# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_NAME=descuentapp
DB_USER=postgres
DB_PASSWORD=postgres123

# Prisma (usa esta variable)
DATABASE_URL=postgresql://postgres:postgres123@postgres:5432/descuentapp?schema=public

# JWT Configuration (opcional para autenticación)
JWT_SECRET=your-super-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# API Configuration
API_VERSION=v1
