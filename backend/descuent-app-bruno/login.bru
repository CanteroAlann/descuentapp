meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{base_url}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email" : "ala@email.com",
    "password" : "1234"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
